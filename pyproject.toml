[project]
name = "bliss"
version = "0.1.0"
description = "LLM heuristic scientists workshop — kitchen scheduling."
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub>=0.24",
    "python-dotenv>=1.0",
]
